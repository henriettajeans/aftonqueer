import { useEffect } from "react";
import { useParams } from "react-router-dom"
import Article from "./article";

export const TheArticle=()=>{
    // const (id) = useParams();
    // useEffect(()=>{
    //     Article(id);
    // });
    return(
        <div> <p>Hej</p>
            {/* {Article.id}*/}
            </div> 
    );
}
